{% extends 'partials/base.html.twig' %}

{% block content %}
  <article class="col-md-9">
    <header>
      {% include 'partials/common/title.html.twig' %}
      <div id="metadata" class="mb-4">
        {% include 'partials/common/date.html.twig' %}
      </div>
    </header>

    <div id="content">
      {{ page.content|raw }}
    </div>

    {# Display taxonomy details #}
    {#% if grav.theme.config.post.taxonomy %}
      <br/>
      {% if page.taxonomy.category %}
        <div class="categories">
          <i class="fas fa-folder"></i>
          {% for category in page.taxonomy.category %}
            <a href="{{ site.blog.route }}/category:{{ category }}">{{ category }}</a>
            {% if not loop.last %},
            {% endif %}
          {% endfor %}
        </div>
      {% endif %}

      {% if page.taxonomy.tag %}
        <div class="tags">
          <i class="fas fa-hashtag"></i>
          {% for tag in page.taxonomy.tag %}
            <a href="{{ site.blog.route }}/tag:{{ tag }}">{{ tag }}</a>
            {% if not loop.last %},
            {% endif %}
          {% endfor %}
        </div>
      {% endif %}
    {% endif %#}

    {% include 'partials/item/navigation.html.twig' %}
  </article>
{% endblock %}
